// Legacy auth context — replaced by /hooks/useAuth (Zustand store).
// Kept as an empty module so any stale imports don't break the build.
// Do not use: import from @/hooks/useAuth instead.
export {};
